import Icon from './Icon';
import Modal from './Modal';

// the equivalent of importing every single element out of Cards and exporting them in one line
export * from './Cards';
export { Icon, Modal };
